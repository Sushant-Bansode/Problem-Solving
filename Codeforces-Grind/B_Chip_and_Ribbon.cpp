#include<bits/stdc++.h>
using namespace std;

 void solve( ){
    int n;
    cin >> n;
    vector<int> arr(n,0);
    for(int i = 0; i<n; ++i){
        cin >> arr[i];
    }

 }

int main( ){
  int t;
  cin >> t;
  while(t--){
    solve( );
  }
}